.card
  .card-block
    .row
      .col-md-12
        p.card-text
          span.text-muted
            = "@#{tweet.user.username}"
            |  at 
            = tweet.timestamp
            |  says
          br
          = tweet.message
